{
  "name": "electron-example",
  "version": "1.0.0",
  "scripts": {
    "start": "ELECTRON_DISABLE_SECURITY_WARNINGS=1 electron-webpack dev",
    "build": "electron-webpack && electron-builder --dir -c.compression=store",
    "test:build": "electron-webpack && electron-builder --dir -c.compression=store -c.mac.identity=null"
  },
  "dependencies": {
    "expo": "^41.0.1",
    "react": "16.13.1",
    "react-dom": "16.13.1",
    "react-native-web": "~0.13.7",
    "source-map-support": "^0.5.16"
  },
  "devDependencies": {
    "electron": "5.0.6",
    "electron-builder": "^21.0.11",
    "electron-webpack": "^2.7.4"
  }
}
